@page

@using HotelManagement6.Areas.Identity.Data
@using Microsoft.AspNetCore.Identity;
@using Microsoft.AspNetCore.Identity.EntityFrameworkCore;
@inject SignInManager<MySqlIdentityUser> SignInManager;
@inject UserManager<MySqlIdentityUser> UserManager;

@model IndexModel
@{
    ViewData["Title"] = "Home page";
}

<div class="text-center">
    <h1 class="display-4">Hotel Management</h1>
    <p>The purpose of this project is for Phillip and Ysani to pass database class and learn asp.net</p>
</div>
@if(User.IsInRole("Admin"))

{
    <h1>hi Admin let me test this out </h1>
}else
{
    <h1>non admin</h1>
}